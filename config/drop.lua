--- Dropping balls: the single drop and the queued multi-drop.
return {
	-- Seconds between balls released from the queue. Range 0.05..5.
	queue_interval = 0.4,

	-- How many balls the multi-drop button queues at once. Range 2..50.
	multi_count = 5,
}
