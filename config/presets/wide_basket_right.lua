--- The wide board with the odds moved to the narrow right basket.
-- Slot 6 is one square of six and a pyramid reaches it about three times in a hundred; here it
-- wins seven times in ten. The sharpest demonstration that the basket is drawn from the weights
-- and the fall only illustrates it.
return {
	rows = 5,
	basket_of_slot = { 1, 2, 2, 2, 2, 3 },
	weights = { 5, 25, 70 },
	scores = { 1000, 10, 1000 },
}
